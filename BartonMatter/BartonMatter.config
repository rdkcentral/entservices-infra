set(autostart ${PLUGIN_USERSETTINGS_AUTOSTART})

if(PLUGIN_BARTONMATTER_STARTUPORDER)
set (startuporder ${PLUGIN_BARTONMATTER_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_BARTONMATTER_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)
